// a function that returns the sum of two numbers

/*
initialize a function that takes 2 parameters (numbers)
return the value of those numbers added together
*/

/*
START

SET sum(number1, number2)
PRINT number1 + number2

END
*/


// a function that takes an array of strings, and returns a string that is all
// those strings concatenated together

/*
initialize a function that take an array as a parameter
set a results variable to empty string
combine the array with .join and store it in the results variable
return results
*/

/*
START

SET stringconcatenater(array)
SET resultsString = ''
resultsString = array join
PRINT resultsString

END
*/

// a function that takes an array of integers, and returns a new array with
// every other element

/*
intialize a function that takes an array as a parameter
set a results variable to []
use the built in filter method to only save all odd index values to results
return results
*/

/*
START

SET everyOther(array)
SET resultsArray = []
resultsArray = array.filter(all odd indexes)
PRINT resultsArray

END
*/